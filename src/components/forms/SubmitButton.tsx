import { Button } from "@components/ui/Button";

const SubmitButton: React.FC = () => {
  return <Button className="mt-8 w-full">Submit</Button>;
};
SubmitButton.displayName = "SubmitButton";

export { SubmitButton };
